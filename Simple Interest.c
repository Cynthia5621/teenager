//calculation of Simple Interest
#include <stdio.h>
 int main(){
	//declaration and initialisation;
	double p = 60000;
	double t = 2;
	double r = 20;
	double interest = (p*r*t)/100;
	printf(" The interest(double )is %lf", interest);
	return 0;
}

